# プライバシーポリシー（Claude使用量チェッカー Chatwork通知版）

最終更新日：2026-07-28

## このプライバシーポリシーについて

「Claude使用量チェッカー（Chatwork通知版）」（以下「本拡張機能」）は、Claude.aiの使用量を表示し、しきい値超過時・平日朝夕にChatworkへ通知するChrome拡張機能です。本ページは、Chromeウェブストアの審査要件に基づき、本拡張機能が扱うデータについて説明するものです。

## 取得する情報とその用途

本拡張機能は以下の情報を扱いますが、**開発者や第三者のサーバーへ送信することは一切ありません。**

| 情報 | 取得元 | 用途 | 保存場所 |
|---|---|---|---|
| Claude.aiの使用量（Current session／All models／モデル別週次制限の各%とリセット時刻） | claude.aiの使用量ページの画面表示（DOM） | ウィジェット表示・Chatworkへの通知文面作成 | ブラウザローカル（`chrome.storage.local`）にのみ一時保存 |
| Chatwork APIトークン・通知先ルームID | ユーザーが拡張機能の設定画面に自分で入力 | Chatworkへの通知送信の認証 | ブラウザローカル（`chrome.storage.local`）にのみ保存。本拡張機能の開発者を含め、Chatwork社以外の第三者には送信されません |
| 日本の祝日データ | 内閣府が公開する祝日CSV（`https://www8.cao.go.jp/chosei/shukujitsu/syukujitsu.csv`） | 平日朝夕レポートの祝日スキップ判定 | ブラウザローカルにキャッシュ（30日ごとに再取得） |

## 送信先

- **Chatwork API（`api.chatwork.com`）**：ユーザー自身が設定したAPIトークン・ルームIDを使い、使用量レポート・しきい値アラートのメッセージ本文のみを送信します。送信先はユーザーが指定したChatworkルームのみです。
- **内閣府（`www8.cao.go.jp`）**：祝日データの取得のみ。個人情報・使用状況データは一切送信しません。
- 上記以外の外部サーバー・分析サービス・広告配信への通信は行いません。

## データの共有・販売

本拡張機能は、取得した情報をいかなる第三者にも販売・共有・提供しません。取得した情報は上記の用途以外に利用しません。

## データの削除

拡張機能をChromeからアンインストールすると、`chrome.storage.local`に保存されていたすべてのデータ（APIトークンを含む）は自動的に削除されます。

## 権限（Permissions）の理由

| 権限 | 理由 |
|---|---|
| `storage` | 設定・キャッシュデータをブラウザローカルに保存するため |
| `tabs` | 使用量ページのタブを検索・作成・フォーカスし、データ取得後に元のタブへ戻すため |
| `alarms` | 5分ごとの自動更新、平日朝夕の定時レポートをスケジュールするため |
| `scripting` | 既に開いているclaude.aiタブへ、表示用スクリプトを注入するため |
| ホスト権限（`claude.ai`） | 使用量ページの画面表示から数値を読み取るため |
| ホスト権限（`api.chatwork.com`） | Chatworkへ通知メッセージを送信するため |
| ホスト権限（`www8.cao.go.jp`） | 祝日データを取得するため |

## 連絡先

本拡張機能に関するお問い合わせ：utuboyauketuke@gmail.com

## ベースとなったソフトウェア

本拡張機能は [ueponx/claude-usage-extension](https://github.com/ueponx/claude-usage-extension)（MIT License）をベースに、Chatwork通知機能を追加したものです。
