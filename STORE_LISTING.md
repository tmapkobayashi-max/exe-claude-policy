# Chromeウェブストア公開用の入力内容（コピペ用）

Chromeウェブストア デベロッパーダッシュボード（https://chrome.google.com/webstore/devconsole）で
新規アイテムを登録するときに、このファイルの内容をそのまま各項目へ貼り付けてください。

## 基本情報

- **名前**：Claude使用量チェッカー（Chatwork通知版）
- **短い説明**（132文字以内）：
  Claude.aiの使用量をブラウザに表示し、しきい値超過時と平日朝夕にChatworkへ自動通知するツールです。
- **カテゴリ**：生産性（Productivity）
- **言語**：日本語

## 詳細な説明

```
Claude.aiの使用量（Current session／All models／モデル別週次制限）を、ブラウザ上のウィジェットで
いつでも確認できます。加えて、以下の通知機能を備えています。

・しきい値通知：使用量が設定した%を超えたら、指定したChatworkルームへ通知
・定時レポート：平日の朝・夕方に、その時点の使用量をChatworkへ自動送信
・通知を止める：「全部止める」（自分で戻すまで）と「今日はもう送らない」（翌朝に自動で戻る）の2通り
・どのアカウントの数字か：画面の表示名を出し、変わったときだけ通知に1行添えます
　（メールアドレスなど、個人を特定できる情報は読み取りません）
　（土日・日本の祝日は自動でスキップ。設定でON/OFF可）
・設定はすべて拡張機能のオプション画面から行います（Chatwork APIトークン・通知先ルームID・
　しきい値・時刻など）
・設定データはブラウザ内にのみ保存され、開発者を含む第三者へ送信されることはありません
　（詳細はプライバシーポリシーをご覧ください）

ueponx氏の claude-usage-extension（MIT License）をベースに、Chatwork通知機能を追加しています。
```

## 単一目的の説明（Single purpose）

```
Claude.aiの使用量を表示し、しきい値超過時・定時にChatworkへ通知することが唯一の目的です。
```

## 権限の使用理由（Permission justifications／Privacy practicesタブ）

- **storage の理由**：
  ユーザーが入力したChatwork接続設定（トークン・ルームID・しきい値・通知時刻）と、
  取得した使用量データをブラウザローカルに保存するために使用します。

- **tabs の理由**：
  使用量ページのタブを検索・一時的に開いてデータを取得し、取得後に元のタブへフォーカスを
  戻すために使用します。ページ内容自体は取得せず、タブの検索・作成・フォーカス切り替え・
  クローズのみに使用します。

- **alarms の理由**：
  5分ごとの自動更新、および平日朝・夕方の定時レポート送信をスケジュールするために使用します。

- **scripting の理由**：
  既に開いているclaude.aiタブに、使用量表示ウィジェット（content.js／floating-widget.js）を
  注入するために使用します。

- **ホスト権限 https://claude.ai/* の理由**：
  使用量ページの画面表示（Current session／All models／モデル別週次制限の%とリセット時刻）を
  読み取るために必要です。

- **ホスト権限 https://api.chatwork.com/* の理由**：
  ユーザー自身が設定したAPIトークンを使い、Chatworkへ通知メッセージを送信するために必要です。

- **ホスト権限 https://www8.cao.go.jp/* の理由**：
  日本の祝日データ（内閣府公表CSV）を取得し、定時レポートの祝日スキップ判定に使うために必要です。

## プライバシーポリシーURL

`PRIVACY_POLICY.md` の内容を、外部から閲覧できるURLとして公開し、そのURLをダッシュボードの
「プライバシーポリシー」欄に入力してください（例：GitHubにpushしてそのURLを使う、
Googleドキュメントで「リンクを知っている全員が閲覧可」に設定して共有する、など）。

## 公開範囲

ダッシュボードの「公開の可視性」で「限定公開（Unlisted）」を選択してください
（検索には出ないが、URLを知っている人は誰でもインストールできる設定）。

## 必要な準備物（あなたが用意するもの）

1. スクリーンショット最低1枚（1280×800 または 640×400 推奨）
   → ウィジェットが表示されているclaude.aiの画面、またはオプション設定画面のスクリーンショット
2. Googleデベロッパーアカウント登録（初回のみ$5）
3. プライバシーポリシーの公開URL（上記参照）
